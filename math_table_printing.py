# Mutliplication tables of the number given in the input
while True:
    a=int(input("Give the number to display the table and press enter or 'press s to exit':  "))
    if a ==('s'):
        exit
    for b in range(1,11):
        print(a,"x",b,"=",a*b)
    
        
    
